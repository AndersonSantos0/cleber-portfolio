import styled from 'styled-components'

export const CertificateItemContainer = styled.div`
  width: 100%;
  aspect-ratio: 4/3;
  background-color: var(--white);
  border-radius: 4px;
  cursor: pointer;
  display: flex;
  overflow: hidden;

  > div {
    width: 100%;
  }
`
